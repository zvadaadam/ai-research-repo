# Research Log

Use this folder for durable observations, explanations, and lessons that should
survive beyond one experiment.

Do not duplicate every run here. Runs belong in `experiments/*/runs/`.

Create a note when:

- a concept becomes clear;
- a visual observation changes interpretation;
- multiple experiments point to one lesson;
- a decision needs durable rationale.
