[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-research-repo"
version = "0.1.0"
description = "Evidence-graph AI research workspace scaffold."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
